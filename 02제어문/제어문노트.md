## 제어문 ( 조건문, 반복문 )

#### 조건문 ( if )
- 조건식은 반환값이 True or False이다.
- 좌중괄호와 우중괄호가 없다.( 내려쓰기와 들여쓰기)
```python
if 조건문:
  print()
```
- if ~ else
- if ~ elif ~ else

#### 반복문( for, while )
```python
for 변수 in 나열가능한 자료:
  실행문1
  실행문2
```